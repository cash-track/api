version: '2.8'

rpc:
  listen: tcp://127.0.0.1:6001

server:
  command: "php app.php"
  relay: pipes

http:
  address: 0.0.0.0:8080
  middleware: [ "gzip" ]
  pool:
    max_jobs: 1
    num_workers: 1
    supervisor:
      max_worker_memory: 100

metrics:
  address: localhost:2112